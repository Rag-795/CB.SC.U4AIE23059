# Campus Notifications Frontend

Frontend for the campus notification platform. Displays all notifications and a priority inbox ranked by category weight (placement > result > event) and recency.

## Setup

```bash
npm install
.env  # Add your auth token in .env file
npm run dev            # Runs on http://localhost:3000
```
